// src/utils/theme.js

const colors = {
  primary: "#557315",       // Main brand color
  secondary: "#FCEADE",     // Light variant
  accent: "#FFD700",        // Highlight (e.g. buttons)
  text: "#333333",          // Default text
  muted: "#777777",         // Secondary text
  background: "#FFFFFF",    // Page background
  cardBackground: "#FAFAFA" // Cards or containers
};

const fonts = {
  heading: "'Poppins', sans-serif",     // Use Google Fonts or local
  body: "'Open Sans', sans-serif",
  decorative: "'Dancing Script', cursive"
};

export { colors, fonts };
